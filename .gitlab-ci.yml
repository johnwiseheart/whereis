image: node:10

stages:
  - deploy

variables:
  APP_NAME: whereis

pages:
  stage: deploy
  script:
    - yarn
    - yarn build-app
    - mkdir -p public
    - cp -r packages/whereis-app/build/* public
  artifacts:
    paths:
      - public
  only:
    - master
